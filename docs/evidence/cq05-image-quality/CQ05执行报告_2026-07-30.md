# C-Q05 真实 Qwen 图片调用执行报告（2026-07-30）

## 执行结论

状态：`BLOCKED`。固定顺序中的第 1 次调用
`ZN-SB-001 / minimal` 通过现有正式 API 发起后失败，系统任务状态为
`failed`。依据“任意一次失败立即停止、不得重试”的授权规则，未执行第 2 至
第 6 次调用。

- 实际 Provider 调用次数：1
- 成功调用次数：0
- 失败调用次数：1
- 成功生成并本地化的结果图：0
- 重试次数：0
- 联系表：未生成；没有结果图可制作联系表
- 人工确认、审批、导出：均未执行
- 安全错误摘要：`Qwen image request failed`
- 任务证据：任务 ID `1`，`status=failed`，
  `model_name=qwen-image-2.0`，`retry_count=0`，
  `result_asset_ids` 为空

该安全错误摘要来自正式任务状态。报告未记录 API Key、完整 API Base、参考图
Base64 或 Provider 临时 URL。

## 调用前检查

| 检查项 | 结果 |
| --- | --- |
| `IMAGE_PROVIDER` | `qwen` |
| `IMAGE_GEN_MODEL` | `qwen-image-2.0` |
| `IMAGE_GEN_OUTPUT_COUNT` | `3` |
| API Base | 已配置；HTTPS 华北 2 Workspace 专属地址校验通过 |
| API Key | 已配置 |
| 结果样本目录 | 调用前图片数为 0；仅存在说明文件 |
| 正式流程 | 当前仓库代码、隔离 PostgreSQL、Alembic、Demo 初始化、正式登录与 `/api/images` API |

旧的已运行 Docker 后端镜像未用于真实调用，因为其运行态 API Base 校验结果不符合
当前华北 2 Workspace 专属域名契约。本次另以当前仓库代码启动隔离正式 API，避免
旧镜像和现有 Demo 数据影响验收。

## 参考图复验

| 商品 | 当前参考图 SHA-256 | 格式 / 尺寸 / 文件大小 | Pillow |
| --- | --- | --- | --- |
| ZN-SB-001 | `FCFBD70C99CB21561BBCA1349B368FF02D669805DE12567AE39BC8F506CF1BF4` | PNG / 1254×1254 / 2,184,678 bytes | decode / verify / reload PASS |
| ZN-DB-002 | `183707A5F50D1706B56C10A58A4DC20B06AB33601CAA9E84A06C613B8C0F8830` | PNG / 1254×1254 / 1,188,682 bytes | decode / verify / reload PASS |
| ZN-VB-003 | `61EEB8B33735FBC27241B69AEB41FA72607C00C81B6C49CFAEEE0FD2C12D9A24` | PNG / 1254×1254 / 1,261,786 bytes | decode / verify / reload PASS |

`ZN-VB-003` 当前文件为产品负责人复核后的替换版本。旧版本因吸尘器吸嘴存在套装
歧义被拒绝，旧 SHA-256
`C93ED705F0F21777D1D6DA5D5C40707EB7C7994069BBB34DBDA8F532C8CC5B4B`
仅作为版本历史保留。

## 固定调用顺序与实际结果

| 序号 | 商品 / 场景 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | ZN-SB-001 / minimal | FAILED | 正式 API 创建任务；Provider 请求失败；立即停止 |
| 2 | ZN-SB-001 / home | NOT EXECUTED | 受第 1 次失败门禁拦截 |
| 3 | ZN-DB-002 / minimal | NOT EXECUTED | 受第 1 次失败门禁拦截 |
| 4 | ZN-DB-002 / home | NOT EXECUTED | 受第 1 次失败门禁拦截 |
| 5 | ZN-VB-003 / minimal | NOT EXECUTED | 受第 1 次失败门禁拦截 |
| 6 | ZN-VB-003 / home | NOT EXECUTED | 受第 1 次失败门禁拦截 |

## 文件与数据库核对

- 参考图路径：[`references/`](references/)
- 结果图路径：[`samples/`](samples/)；当前结果图片数为 0
- 隔离库中登记了 3 个参考图 Asset。
- 隔离库中生成图 Asset 数为 0。
- 运行时上传目录只包含 3 个经过校验的参考图本地副本，没有部分生成图或脏结果
  Asset。
- 由于没有成功结果图，未生成原图清单、逐图 SHA-256 清单或联系表。

## 后续门禁

本轮授权明确禁止重试，因此不得在没有新的产品负责人指令时重新调用第 1 个任务或
继续第 2 至第 6 次调用。`C-Q05` 不得标记为 `PASS`，不得进入 P1。
