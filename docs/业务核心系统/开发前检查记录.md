# P0 开发前检查记录

状态：环境可进入 P0 开发；P0 功能验收尚未通过。  
检查日期：2026-07-21

## 已完成

- 本地 Git 仓库已初始化，默认分支为 `main`，远程地址已指向 GitHub 仓库；尚未提交或推送任何内容。
- Docker PostgreSQL 16 容器 `ecomagent-postgres` 健康运行，开发库为 `ecomagent`。
- 后端虚拟环境与 `requirements.txt` 依赖已安装；FastAPI、SQLAlchemy、psycopg、DashScope 与 pytest 可用。
- 运行时文本与图片 Provider 已切换为 Qwen；真实 Key 保留在被 Git 忽略的 `backend/.env` 中。
- 本机 Docker 的 PostgreSQL IPv6 回环不可用，`DATABASE_URL` 已统一使用 `127.0.0.1`，应用可正常完成启动与建表检查。
- pytest 已排除虚拟环境，并使用明确的当前测试集合；测试 SQLite 夹具已改为 `StaticPool`，保证 FastAPI TestClient 与测试线程共享同一内存数据库。

## 当前验证结果

- 应用启动检查：通过。
- PostgreSQL 容器健康检查：通过。
- Qwen Provider 配置检查：通过，未打印 Key。
- 测试收集：61 项可收集，无重复模块冲突。
- 商品与图片接口窄范围回归：39 项中 31 项通过、8 项失败。
- 内容模板与 LLM mock 测试中还存在 1 项参数展示断言失败。
- 完整回归（已临时隔离真实模型 Key）：61 项中 52 项通过、9 项失败、2 项兼容性警告；9 项失败均归入下方的 P0 开发交接项。

## P0 开发交接项

以下为已有业务代码或旧测试预期的问题，不属于环境准备，需由 P0 开发按 PRD、实现文档和验收文档处理：

1. 商品创建携带库存时，`Inventory` 与新建 SKU 的关联顺序错误，触发 `inventory.sku_id` 非空约束失败。
2. 更新不存在 SKU 的库存接口返回 `200`，预期应为 `404`。
3. 模板内容的 `parameters` 直接返回 JSON 转义字符串，未形成人可读的参数说明。
4. 图片生成接口已要求先上传来源图，但旧测试中仍有“无来源图直接创建任务”的相反预期；需以 P0 图片规则为准统一测试。
5. 图片任务的占位结果与失败路径测试 mock 未完整设置 Provider 条件，导致预期与运行分支不一致。
6. Pydantic 2 兼容性警告尚未清理：类式 `Config` 已弃用，`ImageTaskRead.model_name` 与保护命名空间冲突；当前不阻塞启动，但应在 P0 开发中处理。

## 开发时的最小验证命令

在 `backend` 目录执行。测试时应临时清空 Key，避免 mock 以外的测试产生真实模型调用：

```powershell
$env:LLM_API_KEY = ''
$env:IMAGE_GEN_API_KEY = ''
$env:GOOGLE_API_KEY = ''
$env:DEBUG = 'false'
.\.venv\Scripts\python.exe -m pytest -q
```

修复交接项后，必须重新执行完整测试，并以 [P0 验收文档](验收文档_P0.md) 作为功能验收依据。
