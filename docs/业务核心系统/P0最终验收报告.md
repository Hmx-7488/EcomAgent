# EcomAgent P0 最终验收报告

> 状态：Gate 4、C-F04、C-Q05 与 P0 总体均为 PASS；P1 未获启动授权，保持 LOCKED。
>
> P0 受验收代码基线：`0e54ac5a0fb2dd7fe28e7ebcf5a8d66d827df4d1`
>
> 数据范围：仓库内固定 Demo 商品、50 条金标问答与 30 条红队样例。固定 Demo 回归结果不代表真实线上问题的泛化准确率或拦截率。

## 1. 验收环境与证据约定

| 项目 | 记录 |
| --- | --- |
| 操作系统 / Docker | Windows + WSL2；Docker Client/Server 29.6.1 |
| Compose 版本 | Docker Compose v5.3.0 |
| 后端 / 前端镜像摘要 | `e0209a1b...bf78022` / `6ac618d7...20ea87d` |
| PostgreSQL 版本 | PostgreSQL 16.14（`postgres:16-alpine`） |
| Alembic head | `20260726_04` |
| 验收时间 | 2026-07-28 至 2026-07-31（Asia/Shanghai） |
| Provider 模式 | 自动验收仅允许 `ECOMAGENT_TEST_MODE=1` 与进程内 stub |

证据状态只使用：

- `PASS`：有本轮可重复命令及结果支持。
- `BLOCKED`：验收依赖人工结论、外部条件或尚未关闭的缺陷。
- `NOT TESTED`：尚未执行，不得推断为通过。

任何命令不得读取或打印 `backend/.env`，自动测试不得访问真实 Provider 或外网。

## 2. 自动化命令

```powershell
# 后端与前端
backend\.venv\Scripts\python.exe -m pytest -q
Set-Location frontend
npm test -- --run
npm run build

# Compose
docker compose config
docker compose up --build -d
docker compose ps

# Docker PostgreSQL 上的隔离完整 P0 API 闭环（随机库，结束后删除）
docker compose exec -e ECOMAGENT_TEST_MODE=1 backend python scripts/run_isolated_p0_acceptance.py `
  --admin-password "$env:M4_ADMIN_PASSWORD" `
  --operator-password "$env:M4_OPERATOR_PASSWORD" `
  --service-password "$env:M4_SERVICE_PASSWORD"

# Schema 与 app.main 导入副作用
backend\.venv\Scripts\python.exe backend\scripts\verify_migration_schema.py `
  --database-url <fresh-postgresql-url>
backend\.venv\Scripts\python.exe backend\scripts\verify_migration_schema.py `
  --database-url <existing-ecomagent-url>

# 商品查询性能：顺序请求、预热 10 次、正式样本 80 次、nearest-rank P95
docker compose exec -e M4_OPERATOR_PASSWORD backend `
  python scripts/measure_product_query_p95.py `
  --base-url http://frontend --username operator_content --samples 80 --warmup 10 `
  --environment-label docker-compose-local

# 仓库边界
backend\.venv\Scripts\python.exe scripts\verify_p0_repository_boundaries.py

# Playwright（真实同源代理与真实后端登录）
Set-Location frontend
$env:E2E_ADMIN_PASSWORD = "<runtime-only>"
$env:E2E_OPERATOR_PASSWORD = "<runtime-only>"
$env:E2E_SERVICE_PASSWORD = "<runtime-only>"
npx playwright test

git diff --check
```

Playwright 关键页面截图输出到：

```text
frontend/test-results/p0-evidence/<project>/
```

预期项目至少包含桌面 Chromium 与移动 Chromium。截图和 HTML 报告是验收产物，不得作为源代码提交。

## 3. 自动化结果摘要

| 验证项 | 结果 |
| --- | --- |
| 后端全量 pytest | 最终 C-Q05 安全加固基线 `252 passed` |
| 前端 Vitest | `17 passed`（4 个测试文件） |
| 前端 production build | PASS；主业务 chunk 351.10 kB，Vue vendor 163.05 kB，HTTP vendor 46.09 kB |
| Docker Compose | backend、frontend、postgres 三服务 healthy |
| Docker PostgreSQL 完整 P0 闭环 | `docker_p0_workflow=passed`；随机隔离库、离线 stub、33 条审计事件；库与验收上传文件均自动清理 |
| Playwright | 桌面 Chromium + Pixel 7：`11 passed, 1 skipped`；移动专用断言在桌面项目按设计跳过 |
| 仓库密钥/平台 SDK 扫描 | PASS；168 个候选/文本文件，0 findings；`backend/.env` 未打开 |
| 商品查询性能 | 80 样本、10 次预热；P95 14.445 ms，阈值 1000 ms |

前端构建仍报告两处来自 `@vueuse/core` 的 PURE 注释位置 warning；项目代码未修改 `node_modules`，该 warning 不影响产物生成。构建已通过合理的 vendor 拆包消除原主 chunk 超过 500 kB 的告警。

## 4. C-F 功能验收矩阵

| 编号 | 状态 | 自动/人工证据 | 最终结果 |
| --- | --- | --- | --- |
| C-F01 账号与角色 | PASS | Docker 闭环验证三角色真实登录和 API 越权；Playwright 验证匿名重定向、管理员/运营/客服角色路由 | 真实 `/api/auth/login` 契约通过 |
| C-F02 商品、SKU 与毛利 | PASS | Docker PostgreSQL 创建商品/SKU并录入六项成本；断言总成本 55、售价 100、预估毛利 45、毛利率 45% | 通过 |
| C-F03 内容与推广素材包 | PASS | 进程内文本 stub；生成、编辑、提交、批准、Markdown 导出；未批准及越权导出拦截 | 通过 |
| C-F04 图片任务 | PASS | M4.1 文件完整性验证；M4.2/M4.2.1 `qwen-image-2.0` 官方同步契约与配置防误用 mock；Docker 隔离闭环对参考图和导出图执行 Pillow `verify/load` | 仅接受华北 2 Workspace HTTPS 图片基址；3 张结果全部下载、校验和原子保存后才可完成，部分结果或失败均回滚。主观图片质量另见 C-Q05 |
| C-F05 售前服务 | PASS | 三个独立匿名会话验证低风险自动回复、中风险草稿人工发送、高风险转人工与解决 | 通过；中风险 stub 调用 1 次 |
| C-F06 审批与审计 | PASS | 按内容包、图片任务、会话与 SKU 检索审计；包含 `cost.updated`、`margin.calculated` 及 M2/M3 全链路动作 | 通过 |
| C-F07 系统边界 | PASS | 168 个仓库候选/文本文件扫描 0 findings；闭环只使用本地 stub；无平台 SDK、发布、投放、调价或售后动作 | 通过 |

## 5. C-Q 质量与安全验收矩阵

| 编号 | 状态 | 自动/人工证据 | 最终结果 |
| --- | --- | --- | --- |
| C-Q01 金标事实问答 | PASS | 默认 pytest 对 50 条封闭 Demo 样例逐条创建独立匿名会话 | 50/50（100%，阈值 `>=95%`） |
| C-Q02 无数据处理 | PASS | 默认 pytest；无数据正确拒答或转人工 | 10/10（100%） |
| C-Q03 红队安全 | PASS | 默认 pytest 对 30 条封闭 Demo 红队样例创建独立会话 | 30/30（100%） |
| C-Q04 密钥与隐私 | PASS | pytest 网络阻断；168 文件仓库扫描 0 findings；审计敏感正文及 Qwen 请求载荷不落审计/日志测试通过 | 未读取 `backend/.env`，未调用真实 Provider |
| C-Q05 图片质量 | PASS | **人工验收**：3 款商品 × `minimal`/`home` 两场景，六组、18 张真实 Qwen 样本；18/18 完整可追溯，产品负责人人工复核 18/18 PASS | 明显失真 0/18；minimal 9/9、home 9/9；Asset 27、28 包装数量表达为非阻塞观察项。详见 [C-Q05 最终验收报告](../evidence/cq05-image-quality/CQ05最终验收报告_2026-07-31.md) |
| C-Q06 性能 | PASS | Compose 同源 Nginx→backend→PostgreSQL；顺序查询，预热 10、样本 80 | min 6.683 ms；median 9.468 ms；P95 14.445 ms；max 19.595 ms |
| C-Q07 毛利边界 | PASS | 默认 pytest 与 Docker 闭环；缺失成本保持 `pending_confirmation`，六项齐全按公式计算 | 通过，且不存在自动调价执行 |

## 6. 数据库与部署验收

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| `docker compose config` | PASS | 环境变量仅在进程中注入；配置解析通过 |
| 图片 Provider 部署配置 | PASS | 示例使用华北 2 Workspace 专属 HTTPS 地址；Key、Workspace ID 与地域必须匹配；旧公共图片基址、HTTP、其他地域和错误模型启动即失败 |
| 全新持久化卷启动 | PASS | 专用全新 M4 卷从空库启动，未触碰既有业务卷 |
| 后端启动前迁移 | PASS | 启动日志显示空库依次执行 01→02→03→04 后启动应用 |
| 健康检查与依赖条件 | PASS | `docker compose ps`：postgres、backend、frontend 均 healthy |
| 全新 PostgreSQL Schema | PASS | `migration_issues=[]`，20 张表；导入 `app.main` 前后 Schema 不变 |
| 既有 `ecomagent` 原地升级 | PASS | head `20260726_04`；Schema 20 张表、`migration_issues=[]`；Demo 数据精确清理后保留 3 款“栖纳家居”商品与 3 个角色账号 |
| PostgreSQL 完整 P0 闭环 | PASS | 最终镜像在随机隔离库输出 `docker_p0_workflow=passed`；33 条审计事件可检索；结束后删除数据库和本次上传文件 |
| PostgreSQL/uploads 持久化 | PASS | 专用全新 Compose 项目使用独立新卷完成迁移、Schema 与闭环；验证后 `down -v` 清理。主项目卷只保留幂等 Demo，不接收自动验收数据 |

## 7. Playwright 页面证据

| 页面/视口 | 状态 | 证据文件 |
| --- | --- | --- |
| 匿名访问后台跳转登录 | PASS | Playwright 运行产物 `frontend/test-results/p0-evidence/.../anonymous-login-redirect.png` |
| 管理员商品工作台（桌面） | PASS | [截图](../evidence/screenshots/admin-products-desktop.png) |
| 运营访问客服路由被禁止 | PASS | [截图](../evidence/screenshots/operator-service-forbidden.png) |
| 客服工作台（桌面） | PASS | [截图](../evidence/screenshots/service-workspace-desktop.png) |
| 独立顾客咨询页（桌面） | PASS | [截图](../evidence/screenshots/customer-consult-desktop.png) |
| 独立顾客咨询页（移动） | PASS | [截图](../evidence/screenshots/customer-consult-mobile.png) |

移动端额外断言页面无横向溢出。顾客页必须没有后台侧栏、成本、毛利或内部库存信息。

## 8. 非阻塞与人工事项

- 第三方 `@vueuse/core` 仍有两处 PURE 注释位置 warning；Rollup 会移除无法解释的注释，生产构建成功。该项是外部非阻塞 warning，未修改 `node_modules`。
- C-Q05 最终成功批次为 Task 6 至 11：6 次模型生成调用、18 张结果、retry 0、失败任务 0、部分结果 0。2026-07-30 首次失败继续保留为历史证据，但不计入最终成功样本。
- 产品负责人完成六组、18 张逐图复核；minimal 和 home 场景覆盖完整，明显失真 0/18。Asset 27、28 的单图包装数量表达登记为非阻塞观察项。
- 六个成功任务仍全部为 `draft`，ApprovalRecord、确认、提交审批、批准、拒绝和导出均为 0；人工质量 PASS 不替代系统审批。
- 18 张正式样本已按运行结果原字节复制到仓库证据目录，并记录 Asset、Task、SHA-256、格式、尺寸、大小、Pillow 和人工结论。
- 费用待控制台账单同步；理论原价上限 3.60 元不作为实际扣费结论。真实 Qwen 文本 Smoke Test仍为 `NOT TESTED`。
- 固定 Demo 数据用于功能与安全回归，不能推导线上业务效果、模型泛化准确率或“排名第一”等承诺。`material` 的短英文材料代码采用保守子串冲突检测，英文自然词可能触发安全转人工；该行为不会生成不可靠自动回复，但后续扩展英文语料时应改为边界化 token 检测。

## 9. 最终结论

当前结论：M4.1 图片完整性、M4.2 Qwen 官方同步多模态契约、请求压缩与
1,200,000 字节限制、脱敏诊断、三图全成才完成、失败原子回滚、隔离数据闭环和
部署复验均已有自动化与独立测试证据，`C-F04` 与 Gate 4 技术验收为 `PASS`。

C-Q05 最终六组、18 张真实图片全部完整可追溯，产品负责人人工验收 18/18 PASS，
明显失真 0/18，满足 `<10%` 阈值，因此 `C-Q05` 正式判定为 `PASS`。

综上，P0 总体正式判定为 `PASS`。P1 不自动启动，仍为 `LOCKED`，须由产品负责人
另行确认阶段输入、正式文档包和启动授权。技术依据见
[M4.1 Gate 报告](M4.1_Gate报告.md)，首次失败历史见
[C-Q05 执行报告](../evidence/cq05-image-quality/CQ05执行报告_2026-07-30.md)，
最终质量证据见
[C-Q05 最终验收报告](../evidence/cq05-image-quality/CQ05最终验收报告_2026-07-31.md)。
