# EcomAgent Codex 协作规则

本文件适用于整个仓库。任何 Codex 窗口在分析、修改、测试或调用外部服务前，
都必须先遵守本文件。

## 1. 开工前置

1. 先阅读 [`docs/项目总控.md`](docs/项目总控.md)，确认当前阶段、唯一任务、
   Gate 状态、禁止范围和正式基线。
2. 再按总控页的“必读顺序”阅读正式文档，不得仅凭聊天记忆开工。
3. 首次回复必须先给出项目理解摘要、计划修改范围和明确不修改的范围。
4. 用户或产品负责人确认理解前，不得修改文件、调用真实 Provider、推送或进入下一阶段。
5. 发现聊天、代码、测试、执行报告和正式文档互相冲突时，立即暂停并报告。

## 2. 唯一事实源

信息冲突时按以下优先级处理：

1. 用户最新明确决定。
2. [`docs/需求决策台账.md`](docs/需求决策台账.md) 中最新有效决策。
3. [`docs/项目总控.md`](docs/项目总控.md) 的当前状态与门禁。
4. 当前阶段正式 PRD、架构、实现和验收文档。
5. Git 提交、测试输出和验收证据。
6. 聊天记录。
7. Obsidian 或其他个人笔记。

`docs` 是正式项目文档源。Obsidian 只允许保存状态摘要和链接，不得形成另一套需求。
聊天不能替代正式文档。

## 3. 固定产品边界

- 单商家、单实例、Docker 私有化部署，明确不做 SaaS、多租户或共享商家实例。
- P0 仅覆盖商品、SKU、成本与预估毛利、内容与图片候选、审批审计和受控售前客服。
- P0 不接淘宝、拼多多、抖音、小红书等平台，不包含平台 SDK、真实订单同步、
  发布、投放、自动调价、退款或售后执行。
- 业务核心与渠道接入中心是同仓库内的两个独立系统，通过内部 API 和异步事件通信，
  不共享数据库；渠道能力从 P2 起按合法授权逐 Connector 建设。
- 所有图片、内容和未来写操作都必须保留人工确认；不得承诺链接排名第一。
- 阶段边界和后续范围以 [`docs/后续阶段路线图.md`](docs/后续阶段路线图.md) 为准。

## 4. 窗口角色边界

### 产品总控窗口

- 负责需求澄清、范围冻结、任务下发、产品验收、风险判断和 Gate 决策。
- 不修改后端、前端、迁移、测试或执行脚本，不直接调用真实 Provider。
- 可以维护项目治理、PRD、路线图、决策台账和验收结论文档。

### 开发执行窗口

- 只实现已批准任务单，不自行增加功能、角色、接口、Provider 或阶段范围。
- 修改前报告文件所有权和接口影响；完成后报告测试、迁移、风险和 Git 范围。
- 发现必须扩大范围或修改已冻结契约时，停止并交回产品总控决策。

### 测试验收窗口

- 负责独立复验、缺陷报告、证据整理和验收映射，不擅自修复业务实现。
- 自动测试必须隔离真实 Key 和外网；真实 Provider 调用必须有用户针对本批次的明确授权。
- 真实调用执行“失败即停、不得静默重试”，实际次数、成功数、费用和错误摘要必须留证。

同一窗口不得在未声明和未获确认的情况下切换角色。

## 5. 安全与变更规则

- 不读取、打印、提交或在报告中复述 `.env`、API Key、Token、数据库密码和完整私有地址。
- 只允许报告敏感配置是否存在、是否通过格式校验和所属地域是否匹配。
- 不得修改或提交用户未授权的既有改动；不使用破坏性 Git 或文件命令。
- 迁移必须追加，禁止改写已发布的 01、02、03、04 历史迁移。
- 任何真实 Provider 重试、扩样或费用增加都需要新的明确授权。
- 未满足当前 Gate 时，不得把状态写为 `PASS`，不得进入下一阶段。

## 6. 交付与交接

每次执行报告至少包含：

- 当前分支、起始 Commit 和结束 Commit。
- 实际修改文件、明确排除范围和 Git 状态。
- 测试命令、通过数、失败数、warning 和未执行项。
- 数据库迁移、Docker、外部 Provider 和费用情况。
- 当前 Gate 判定、遗留风险和下一步所需授权。

状态发生变化后，执行窗口只能提出 [`docs/项目总控.md`](docs/项目总控.md) 更新建议；
由产品总控核对并更新。新窗口必须重新从总控页开始，不继承旧窗口的口头结论。
